# STATUS — текущее состояние проекта maria_doll

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-10 (Этап 7 закрыт вживую — прод на сервере с выделенным IP, HTTPS, автодеплой)
**Текущий этап**: между этапами — Этапы 0–7 закрыты. Следующий — Этап 8 (Реккуренты Продамус), отложен до одобрения Продамусом.
**Прод-конфигурация:** сервер `87.58.210.25:22` (выделенный IP, root), деплой `/opt/maria_doll` (стек pg16+redis+bot+admin). Админка `https://dolbikfit.ru` (Let's Encrypt, автопродление), вебхук `https://dolbikfit.ru/prodamus/webhook`. CI/CD: push в `master` → GitHub Actions → автодеплой. Deploy-ключ/`.env` — в `.secrets/`. Логин админки `admin`/`admin123` — **сменить пароль** (домен публичный).
**Открытые хвосты (не блокеры этапа 7):** (1) прописать в ЛК Продамуса боевой «URL для уведомлений» = `https://dolbikfit.ru/prodamus/webhook` + тестовая оплата; (2) удалить старые серверы `.8`/`.130` у провайдера; (3) сменить дефолтный пароль админки `admin123`. Фикс «Продлить подписку» в «Моя подписка» — ✅ выкачен в прод (master `3bf59fd`, автодеплой).
**Следующий шаг**: деплой бота и админки на сервер `87.58.210.8:41480` (навык `/okdeploy`), домен `dolbikfit.ru` под админку с HTTPS, CI/CD на `master`. DNS `dolbikfit.ru`+`www` уже указывают на `87.58.210.8`. На проде nginx проксирует ДВА апстрима: `/` → админка (127.0.0.1:8011), `/prodamus/webhook` → бот (127.0.0.1:8080). При деплое перенести `S3_*`, `ADMIN_WEB_*`, `PRODAMUS_SECRET_KEY` в серверный `.env`/`.secrets`. Перед стартом серверного бота — остановить локальный стек (конфликт polling по токену). Критерии — в [`07_ROADMAP.md`](07_ROADMAP.md#этап-7--деплой-okdeploy).
**Боевая конфигурация восстановлена:** первый тариф = 990 ₽ / 1 месяц, `PRODAMUS_DEMO_MODE=false`, `EXPIRY_CHECK_INTERVAL_MIN=30`, `REMINDER_CHECK_INTERVAL_MIN=60`, `REMINDER_UNIT=day` (пороги 3/1/0), `CLUB_CHAT_ID=-1004324451388`. Продамус: для боя — выключить демо на странице и прописать боевой «URL для уведомлений» (постоянный, после деплоя — этап 7; тестовый туннель cloudflared — временный, погашен после теста).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Фундамент | ✅ | `stage-0-done` | `56a5825` | 2026-07-07 |
| 1 | Каркас бота | ✅ | `stage-1-done` | `46a102b` | 2026-07-07 |
| 2 | Оплата Продамус (разовая) | ✅ | `stage-2-done` | `73e16e7` | 2026-07-07 |
| 3 | Управление доступом в чат | ✅ | `stage-3-done` | `73e16e7` | 2026-07-07 |
| 4 | Уведомления и напоминания | ✅ | `stage-4-done` | `40d0411` | 2026-07-08 |
| 5 | Промокоды | ✅ | `stage-5-done` | `cbca4f3` | 2026-07-08 |
| 6 | Веб-админка | ✅ | `stage-6-done` | `a2bf303` | 2026-07-08 |
| 7 | Деплой (okdeploy) | ✅ | `stage-7-done` | `b52a7de` | 2026-07-10 |
| 8 | Реккуренты Продамус (позже) | ☐ | `stage-8-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Этап 7 — Деплой (okdeploy).** ✅ Закрыт вживую (2026-07-10). Прод на выделенном сервере после эпопеи с NAT: `.8` (NAT-VPS — входящие 80/443 недоступны в принципе, тупик) → `.130` (выдан, но по сети не поднялся) → **`87.58.210.25`** (рабочий выделенный IPv4, публичный прямо на eth0). Самодостаточный стек `docker-compose.yml` (pg16+redis+bot+admin), 10 миграций, схема `maria_doll`. Nginx на `dolbikfit.ru` (Let's Encrypt, HTTP→HTTPS 301, автопродление) проксирует `/`→админка (8011) и `/prodamus/webhook`→бот (8080). CI/CD `deploy.yml`: push в `master` → GitHub Actions → SSH → `git reset` + `docker compose up -d --build`; проверено тестовым выкатом (v0.1.0→v0.1.1, коммит `b52a7de`, бот перезапустился, БД цела). Ключевые уроки: (1) NAT-VPS не годится для сайта/вебхука — нужен выделенный IPv4; (2) polling-боту NAT не мешает (исходящее); (3) при смене IP менять A-запись (reg.ru) + GitHub Secrets (SERVER_HOST/PORT). Deploy-ключ/`.env` — в `.secrets/` (`deploy_key`, `server.env`). Открытые хвосты (не блокеры): вебхук в ЛК Продамуса, удаление старых серверов `.8`/`.130`, выкат фикса «Продлить» (в `dev`).

**Этап 5 — Промокоды.** ✅ Закрыт вживую (2026-07-08, Telegram Web). Адаптация под фиксированные тарифы: промокод применяется к выбранному тарифу на экране сводки. Типы `percent`/`fixed`; таблицы `promo_codes` + `promo_redemptions` + `payments.promo_id`; активация пишет redemption + инкремент `used_count` (идемпотентно). Создание кодов из админки — этап 6.

**Этап 4 — Уведомления и напоминания.** ✅ Закрыт вживую (2026-07-08) на короткой подписке 10 мин. Напоминания early/soon/last (пороги в `bot_settings`, дефолты из `.env`), джоб `run_reminder_check`, защита от дублей через `subscription_reminders`. Рабочее продление: `NAV_RENEW` + продление от текущей `end_date` + guard кика в `run_expiry_check`. Уведомление «подписка закончилась» + закрытие доступа — с этапа 3.

**Этап 3 — Управление доступом в чат.** Код готов и локально проверен. Перенесены из аналога: `handlers/join.py` (авто-одобрение join-request по активной подписке, decline без неё), `services/subscriptions.py` (кик из группы ban+unban, `run_expiry_check`), `repo.expire_due_subscriptions`, экран «Моя подписка» (`handlers/subscription.py`), клавиатуры/тексты доступа. APScheduler в `main.py` крутит проверку окончаний каждые 30 мин; в allowed_updates появился `chat_join_request`. Smoke (Docker): нет подписки→не активна, активная→видна, истёкшая→`expired` и не активна, повтор проверки идемпотентен. **Живьём (в конце):** нужен бот-админ в закрытом чате `-1003546154644` + инвайт «по заявке» — тогда заявка оплатившего одобряется, по окончании — кик.

**Этап 2 — Оплата Продамус (разовая).** ✅ Закрыт вживую (демо-оплата тестовой картой через туннель cloudflared). Ссылка payform (с подписью), приём вебхука `src/webapp.py` (aiohttp, `/prodamus/webhook`), проверка Hmac-подписи, идемпотентная запись подписки, уведомление пользователя. Ключевой фикс, найденный на живом тесте: экранирование слэшей в строке подписи (`sign_payload`) — PHP `json_encode` Продамуса экранирует `/`→`\/`, из-за чего подпись расходилась на `payment_type "Visa/MasterCard/МИР"`.

## Известные блокеры

- **Живой тест этапа 3** — нужен бот-админ в закрытой группе (с правом одобрять заявки и банить) + инвайт «по заявке». Клиент включил заявки, ссылка `https://t.me/+DaALCOx8Wh43OGQy` (в `.env`). После — заявка оплатившего одобряется, по окончании подписки — кик.
- **Реккуренты Продамуса (этап 8)** — Продамус подключит клубный функционал/выдаст документацию только под готового бота. Пока разовый платёж. **Согласование идёт:** заявка #400-668250 (prodamus.omnidesk.ru/user/cases/record/400-668250). 1-е письмо (5 пунктов): сайт приёма оплаты → payform + `t.me/Fit_it_bot` + `dolbikfit.ru`; описание услуг; оборот (данные заказчицы); оферта; **Sys** = `fititclub`. **2-е письмо Продамуса** (требования к боту-ресурсу): (а) в боте разместить оферту (ссылкой) + реквизиты ИП (ФИО/ИНН/контакты) + описание/состав/стоимость/тариф услуги; (б) ресурс приёма платежей должен быть **в общем доступе и рабочим** — «тестовый (незапущенный) ресурс не принимаем»; (в) оферта на ВСЕХ ресурсах приёма, не только на payform; (г) в самой оферте — описание/тарифы + как отменить подписку + условия возврата. **Сделано в боте** (команды `/oferta` — описание+тарифы из БД+реквизиты ИП+ссылка на оферту S3, `/sub` — статус, меню команд): закрывает (а). **Оферта под рекуррент — ДОПОЛНЕНА** (Продамус: перегенерить нельзя, правки вносим сами): добавлен раздел «VБ. Регулярные (рекуррентные) платежи и автопродление подписки» (согласие на автосписания, периодичность=тариф, автопродление, 2–3 попытки при неуспехе, отмена через бот/email, возврат, уведомления) + правки п.11.4/11.5 (оговорка про подписку вместо «разового характера»). Готовый файл: `Downloads/dolbikfit.payform.ru_public_offer_recurrent.docx` (+`.txt`; оригинал `.pdf` сохранён). Из DOCX заказчица сохраняет в PDF через Word. Осталось: заказчице отправить дополненную оферту Продамусу на проверку достаточности формулировок; форма payform уже переведена в БОЕВОЙ режим (в боте `PRODAMUS_DEMO_MODE=false` подтверждён). Механизм отмены подписки в боте (кнопка) — реализовать в этапе 8. Разблокировка этапа 8: одобрение Продамуса + их документация по способу 3 (свой робот-списание).

## История закрытий

- 2026-07-10 — Этап 7: Деплой (okdeploy) — tag `stage-7-done` — Прод на выделенном сервере `87.58.210.25` (Ubuntu 24, выделенный IPv4). Самодостаточный `docker-compose.yml` (pg16+redis+bot+admin) + `scripts/install.sh` + `deploy/nginx/dolbikfit.ru.conf` + CI/CD `.github/workflows/deploy.yml` — адаптация эталона `club_bot` под схему `maria_doll` и Продамус-вебхук. Nginx на `dolbikfit.ru` под HTTPS (Let's Encrypt, HTTP→HTTPS, автопродление) проксирует `/`→админка (127.0.0.1:8011) и `/prodamus/webhook`→бот (127.0.0.1:8080). Push в `master` → GitHub Actions → автодеплой (проверено выкатом v0.1.1 `b52a7de`, БД сохранена). Провайдер rdp-onedash: первый сервер был NAT-VPS без входящих 80/443 (тупик для сайта/вебхука) — переехали на выделенный IPv4 (менялись A-запись reg.ru и Secrets SERVER_HOST/PORT). Проверено вживую заказчицей: админка `https://dolbikfit.ru`, бот `@Fit_it_bot`. Хвосты: боевой «URL для уведомлений» в ЛК Продамуса, удаление старых серверов `.8`/`.130`, выкат фикса «Продлить» из `dev`.
- 2026-07-08 — Этап 6: Веб-админка — tag `stage-6-done` — Перенос `src/webadmin/*` из аналога `club_bot` на FastAPI+Jinja2 с вырезанием билетов/событий/рефералки/чек-ина и адаптацией под нашу схему (фиксированные тарифы, promo percent/fixed, доход по `paid_at`, `tg_id`). Отдельный процесс `uvicorn` (сервис `admin`:8000 в dev-compose), общая БД с ботом; фактические действия в Telegram (кик, рассылка) делает бот фоновыми джобами. Разделы: логин/сессия (cookie, pbkdf2, сид первого админа из `ADMIN_WEB_*`), дашборд (доход за период со сравнением, активные/скоро заканчивается/платежи, SVG-график + переключаемые графики регистраций/активности/распределения подписчиков — только подписки), подписки (список + продление/отключение, карточка участника с историей, ручная выдача/аннулирование), тарифы (фиксированные — CRUD, вкл-выкл), промокоды (percent/fixed — создание/вкл-выкл/удаление), рассылки по сегментам (текст + до 10 фото; очередь `broadcasts` + бот-джоб `services.broadcasts`: pending→sending→done, счётчики sent/blocked/failed, отметка заблокировавших), настройки (пороги напоминаний, ссылка поддержки → редактируемые `bot_settings`, FSM «кто на каком экране»), экраны бота (текст + картинка) и кнопки меню. **Картинки через S3** (Beget, ключи из референса): `services/storage.py` (boto3), фото в рассылках (одно → `send_photo`, несколько → `media_group`) и на экранах (`screen_texts.photo_url`); при пустом S3 — только текст. Раздел «доп-админы» аналога выкинут (`admin_id_list` в боте не используется). Миграции `0009_admin_broadcasts` (admin_users + broadcasts) и `0010_broadcast_photos` (broadcasts.photos JSON). Фикс двойной отправки формы рассылки при заливке фото (guard в base.html). Smoke (Docker): все разделы 200, вход/редирект; создание промокода, правка тарифа (990→1234→990) и текста экрана, toggle/delete промо, настройки/кнопки — отражаются в БД; активные подписчики на дашборде = SQL; текстовая рассылка бот-джобом (sent 2/2). Проверено вживую: вход admin/admin123, правки → в боте, рассылка по сегменту; **S3** — картинка экрана (публичный URL 200) и рассылка с 2 фото альбомом (sent 2/2, фото 2). S3-ключи в локальном `.env` — на прод-деплое (этап 7) перенести в серверный env/`.secrets`.

- 2026-07-08 — Этап 5: Промокоды — tag `stage-5-done` — Адаптация под фиксированные тарифы (модель аналога со ставкой ₽/мес и матрицей цен не применима): промокод применяется к выбранному тарифу на экране сводки. Поток: тариф → сводка → «Ввести промокод» → FSM ждёт код (tariff_id в контексте) → валидация (not_found/expired/exhausted/already_used) → пересчёт суммы → сводка со спец-ценой → оплата (платёж с `promo_id`). Типы: `percent` (скидка %) и `fixed` (спец-цена ₽). Сумма пересчитывается на сервере от цены тарифа (клиенту не доверяем); код перепроверяется на момент оплаты. Таблицы `promo_codes` + `promo_redemptions` (уникум promo+юзер — один код одна активация) + `payments.promo_id`; при успешной активации — redemption + инкремент `used_count` в транзакции `activate_payment` (идемпотентно). Миграция `0008_promocodes`; новое: `services/promo.py`, `states.py`, `handlers/promo.py`. Smoke (Docker): расчёт percent/fixed; статусы; 990→792 при 20%; redemption + used_count+1; already_used; идемпотентность. Проверено вживую в Telegram Web: `ВЕСНА20` −20% → 792 ₽, `VIP500` fixed → 500 ₽, отклонение истёкшего/исчерпанного/несуществующего. Создание кодов из админки — этап 6.

- 2026-07-08 — Этап 4: Уведомления и напоминания — tag `stage-4-done` — Перенос из аналога напоминаний «скоро продление»: три порога early/soon/last (в `bot_settings`, дефолты из `.env`: day/3/1/0), джоб APScheduler `run_reminder_check` (дефолт каждые 60 мин), защита от дублей через `subscription_reminders` (atomic claim ON CONFLICT DO NOTHING; за проход шлём только самое срочное, «пропущенные» окна занимаем без рассылки). Уведомление «подписка закончилась» + закрытие доступа — с этапа 3. По решению заказчика сделано **рабочее продление**: `NAV_RENEW` открывает тарифы активным подписчикам; `activate_payment` отсчитывает новый срок от текущей `end_date` (оплаченные дни не теряются), `start_date` = момент оплаты; guard в `run_expiry_check` не кикает продлившего (истекла старая, но есть действующая). Затронута платёжка этапа 2 (`activate_payment`, `pay_create`, `start_payment` kind=renewal). Миграция `0007_reminders`: `bot_settings` + `subscription_reminders`. Smoke (Docker): early→идемпотентность→last→всё занято; пороги из БД на лету; продление от конца текущей; guard кика. Проверено вживую на короткой подписке 10 мин (`REMINDER_UNIT=minute`, пороги 5/2/0): early→soon→last→«закончилась»+кик; продление активной подписки — доступ не потерян.

- 2026-07-07 — Этап 3: Управление доступом в чат — tag `stage-3-done` — Перенесены из аналога `handlers/join.py` (авто-одобрение `chat_join_request` при активной подписке, decline без неё), `services/subscriptions.py` (кик ban+unban, `run_expiry_check`), `repo.expire_due_subscriptions`, экран «Моя подписка». APScheduler крутит проверку окончаний; `chat_join_request` в allowed_updates. Проверено вживую на группе `-1004324451388`: оплативший подал заявку → бот одобрил; подписка истекла → бот кикнул; неоплативший подал заявку → отказ. Диагностический лог заявки из другого чата помог найти смену id группы клиентом. Кик = ban+unban (место освобождается, вернуться можно новой оплатой).
- 2026-07-07 — Этап 2: Оплата Продамус (разовая) — tag `stage-2-done` — Ссылка на payform (с подписью), приём вебхука на aiohttp-эндпоинт `/prodamus/webhook`, проверка Hmac-подписи, идемпотентная запись подписки, уведомление пользователя. Ключевой баг найден на живом тесте: подпись расходилась из-за экранирования слэшей (`/`→`\/`) — PHP `json_encode` Продамуса экранирует, Python нет; поправлено в `sign_payload`. Проверено вживую демо-оплатой (тестовая карта Газпромбанка через туннель cloudflared): вебхук принят, подпись сошлась, подписка создана (55 ₽, 3 мин), уведомление доставлено; фоновая проверка окончаний пометила `expired`. Реккуренты — этап 8.
- 2026-07-07 — Этап 1: Каркас бота — tag `stage-1-done` — Перенесены из аналога и адаптированы навигация/экраны/меню/тарифы. Всё редактируемое содержимое — в БД: тексты экранов (`screen_texts`), кнопки меню (`menu_buttons`), тарифы (`tariffs`, сид 990/2370/3540/5990), журнал действий (`user_events`). Ключевое упрощение: вместо скрытых авто-ступеней аналога — простые фиксированные тарифы. Smoke-тест в контейнере подтвердил: экраны/тарифы/кнопки читаются из БД, правка текста и цены через SQL отражается сразу без рестарта. Заказчица проверила бота вживую (`/start`, тарифы, навигация). Оплата — заглушка (кнопка «Перейти к оплате» ведёт на «скоро»), реальная оплата на этапе 2. `get_active_subscription` — заглушка (None) до появления таблицы подписок на этапе 2.
- 2026-07-07 — Этап 0: Фундамент — tag `stage-0-done` — Перенесён скелет из аналога `club_bot` (config/db/cache/logger/migrate/main/middlewares), адаптирован под схему `maria_doll` и Продамус. Бот собрался в Docker и залогинился в Telegram (`@Fit_it_bot`, polling), миграция `0001_init` создала таблицы `users`/`fsm_states` в схеме `maria_doll` внутри БД `mydb`; чужая схема `hermes_agent` не тронута (её таблица `content_drafts` на месте). Заказчица проверила `/start` вживую — заглушка приходит. Решение по Продамусу уточнено: подтверждение оплаты будет **вебхуком** (не опросом).

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит», «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.

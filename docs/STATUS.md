# STATUS — текущее состояние проекта maria_doll

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-07-07 (взят в работу Этап 0 — Фундамент)
**Текущий этап**: Этап 0 — Фундамент (🚧 в работе)
**Следующий шаг**: перенести и адаптировать скелет из аналога `club_bot` (config/db/migrate/logger/main), поднять локально в Docker со схемой `maria_doll`. Критерии — в [`07_ROADMAP.md`](07_ROADMAP.md#этап-0--фундамент).

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Фундамент | 🚧 | `stage-0-done` | — | — |
| 1 | Каркас бота | ☐ | `stage-1-done` | — | — |
| 2 | Оплата Продамус (разовая) | ☐ | `stage-2-done` | — | — |
| 3 | Управление доступом в чат | ☐ | `stage-3-done` | — | — |
| 4 | Уведомления и напоминания | ☐ | `stage-4-done` | — | — |
| 5 | Промокоды | ☐ | `stage-5-done` | — | — |
| 6 | Веб-админка | ☐ | `stage-6-done` | — | — |
| 7 | Деплой (okdeploy) | ☐ | `stage-7-done` | — | — |
| 8 | Реккуренты Продамус (позже) | ☐ | `stage-8-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Этап**: 0 — Фундамент, в работе.
**Ветка**: `dev`
**Сводка задач**: перенос скелета из аналога (config/db/cache/logger/migrate/main/middlewares), адаптация под схему `maria_doll` и Продамус; docker-compose.dev.yml с подключением к уже запущенным на ПК pg (`postgres16`, БД `mydb`) и redis через `host.docker.internal`; alembic + миграция `0001_init` (users + fsm_states); минимальный `/start`-стаб.
**Как верифицируем**: `docker compose -f docker-compose.dev.yml up --build` поднимает бота без ошибок; `/start` отвечает в Telegram; в `mydb` появилась схема `maria_doll` с таблицами миграции; чужая схема `hermes_agent` не тронута.

## Известные блокеры

- **Реккуренты Продамуса (этап 8)** — Продамус подключит клубный функционал/выдаст документацию только под готового бота. Пока делаем разовый платёж. Разблокировка: одобрение Продамуса + их документация по способу 3.

## История закрытий

Пока пусто.

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег». Тег без записи в таблице = таблица устарела, обновить.
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Убедиться, что все чекбоксы в `07_ROADMAP.md` отмечены `[x]` и реально проверены.
2. Создать тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, «Закрывающий тег», «Последний коммит», «Дата закрытия».
4. Добавить запись в «Историю закрытий».
5. Указать новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `07_ROADMAP.md` и спецификации в `docs/`.
- TODO внутри текущего этапа — это таск-лист активной сессии.
- История изменений кода — это `git log`.

Этот файл — **только снимок текущего состояния по этапам**. Должен помещаться на один экран.
